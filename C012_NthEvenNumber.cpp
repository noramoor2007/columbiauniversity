#include <iostream>
// Create a function that takes a number n and returns the nth even number beginning with 0 as the first.
int nthEven (int n) {
	return (n - 1) * 2; // It is always the number minus 1 multiplied by 2 because 0 counts as an even number.
}