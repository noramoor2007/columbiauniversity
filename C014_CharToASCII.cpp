#include <iostream>
// Create a function that returns the ASCII value of the passed in character.
unsigned short ctoa (char c) {
	return int (c); // You can find the ASCII value simplying by putting the input variable in the parenthesis of the data type "int".
}